########################################################################
# Spring
########################################################################
spring:
  output:
    ansi:
      enabled: detect
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  config: classpath:logback-local.xml
  level:
    ca:
      qc:
        plachanc73: debug
    org:
      hibernate:
        SQL: debug
